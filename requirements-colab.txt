# Minimal runtime dependencies for Google Colab on Python 3.12.
# NumPy/SciPy/Pandas are pinned together to avoid binary ABI mismatches in Colab runtimes.
# Numba is upgraded because older Colab builds reject NumPy 2.x.
numpy==2.2.6
scipy==1.15.3
pandas==2.2.3
numba>=0.61.2
wntr>=1.3
viswaternet>=2.2
ptitprince>=0.2.7
openpyxl>=3.1
